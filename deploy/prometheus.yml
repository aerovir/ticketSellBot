global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "ticketbot-web"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["web:8080"]

  - job_name: "postgres-exporter"
    static_configs:
      - targets: ["postgres-exporter:9187"]
