# User Flows — канонические сценарии TicketBot

**Дата:** 2026-08-12
**Назначение:** единая основа для e2e-тестов и подключения новых фич. Каждый flow — это сквозной сценарий пользователя от входа до результата, с указанием роли, платформ, шагов и текущего статуса.

---

## Роли (терминология — CLAUDE.md)

| Роль | Кто | Ключевое |
|------|-----|----------|
| **Пользователь** | Посетитель | Покупает билеты, получает код/QR, предъявляет на входе |
| **Организатор** | Клиент бота | Создаёт мероприятия, продаёт, проверяет билеты на входе, смотрит статистику. Подписка (на канал или на пользователя). Может работать без канала (Mini App). |
| **Супер-админ** | Владелец бота | `ADMIN_TELEGRAM_IDS`. Глобально: подписки каналов, все каналы/мероприятия, рассылка, статистика, здоровье. Не привязан к каналу. |

---

## Матрица flows

| # | Flow | Роль | TG | VK | Статус | e2e-покрытие |
|---|------|------|----|----|--------|--------------|
| **F1** | Покупка билета | Пользователь | ✅ | ✅ | ✅ реализовано | `test_telegram_sim`, `test_vk_e2e` (+ `test_vk_buy_refund_sync_to_tg`) |
| **F2** | Мои билеты + QR | Пользователь | ✅ | ✅ | ✅ реализовано (#8). **Предъявление:** бесплатный → код, платный → QR (A) | ✅ e2e `TestCabinetFlow` (web + QR) |
| **F3** | Возврат билета | Пользователь | ✅ | ✅ | ✅ реализовано | ✅ e2e `TestCabinetFlow` (refund + место) + `test_vk_e2e.test_vk_buy_refund_sync_to_tg` (VK-возврат → TG-статистика) |
| **F4** | Создание мероприятия | Организатор | ✅ | ✅ | ✅ реализовано. **Лимит (B):** free-организатор — 1 опубликованное будущее, 409 при превышении | `test_organizer_e2e` + `TestCabinetFlow::test_free_organizer_one_event_limit` |
| **F5** | Публикация анонса (стена группы) | Организатор | ✅ | ✅ | ✅ реализовано | ✅ e2e VK + ✅ TG-sim (репост анонсов) |
| **F6** | Продвижение: рассылка в ЛС подписчиков | Организатор | ⏸️ | ⏸️ | 📌 Фаза 2 (не реализовано) | — |
| **F7** | Проверка билета на входе (check-in) | Организатор | ✅ | ✅ | ✅ реализовано. **Способы:** ручной ввод кода или QR-сканер (камера/фото, авто-check-in, #94) | ✅ e2e VK (обе стороны: VK→TG и TG→VK `test_tg_buy_vk_checkin_symmetry`) + ✅ TG-sim (`/check`) + ✅ `TestCabinetFlow` (validate→event_id + checkin) + ✅ структурные smoke `test_frontend`. **Камера** — мануальный тест в WebView (Android TG — фото-фоллбек) |
| **F8** | Линковка TG↔VK (идентичность) | Организатор | ✅ | ✅ | ✅ реализовано (#160) | `test_vk_e2e`, `test_identity` |
| **F9** | Статистика продаж | Организатор | ✅ | ✅ | ✅ реализовано | `test_organizer_e2e` |
| **F10** | Подписка организатора (basic/pro) | Организатор | ✅ | ✅ | ✅ реализовано | `test_organizer_e2e` |
| **F11** | Управление каналами (самообслуживание) | Организатор | ✅ | ✅ | ✅ реализовано | `test_organizer_e2e` |
| **F12** | VK-группы (self-service) | Организатор | ✅ | ✅ | ✅ реализовано (#163) | `test_vk_groups` |
| **F13** | Подписка каналов (глобально) | Супер-админ | ✅ | ⏸️ | ✅ реализовано | ✅ e2e `test_super_admin_e2e` |
| **F14** | Список/инфо каналов, смена админа | Супер-админ | ✅ | ⏸️ | ✅ реализовано | ✅ e2e `test_super_admin_e2e` |
| **F22** | Выдача подписки организатору без канала | Супер-админ | ✅ | ⏸️ | ✅ реализовано. `POST /admin/users/{telegram_user_id}/subscription` (дни + tier). Вход: главная → «👥 Пользователи» (подписать из карточки) или Панель → «Инфо о юзере» | `TestAdminUserSubscribe` (web) + frontend smoke |
| **F15** | Глобальная статистика | Супер-админ | ✅ | ⏸️ | ✅ реализовано | ✅ e2e `test_super_admin_e2e` |
| **F16** | Рассылка (broadcast) | Супер-админ | ✅ | ⏸️ | ✅ реализовано | ✅ e2e `test_super_admin_e2e` |
| **F17** | Здоровье / мониторинг | Супер-админ | ✅ | ⏸️ | ✅ реализовано | ✅ e2e `test_super_admin_e2e` |
| **F18** | Пригласительные (invite-ссылки, pro) | Организатор | ✅ | ✅ | ✅ реализовано | ✅ e2e `test_organizer_e2e` (owner-путь: `test_invites_not_counted_in_sold`; канал: `test_channel_organizer_invites_and_qr`); активация гостем: `TestCabinetFlow::test_invite_claimed_by_guest_via_link` |
| **F19** | Per-event премиум (единовременная оплата) | Организатор | ✅ | ✅ | ✅ реализовано | ✅ e2e `TestCabinetFlow::test_event_premium_unlocks_paid_features` + `TestEventUpgrade` (services) |
| **F20** | Скидки-промокоды (pro) | Организатор | ✅ | ✅ | ✅ реализовано. Промокод на конкретное мероприятие: код, тип скидки (percent/fixed), срок (starts_at/ends_at), лимит (max_uses), вкл/выкл. Покупатель вводит при покупке → скидка в цене. **Возврат билета НЕ возвращает слот использования промокода** | ✅ e2e `test_promo_e2e.py` (полный цикл: создать → купить со скидкой → used_count → лимит) + `TestPromoCodes` (services) + `TestPromoCodesAPI` (web) |
| **F21** | Динамические цены по дате (pro) | Организатор | ✅ | ✅ | ✅ реализовано. Ценовые диапазоны с абсолютными датами (сплошное покрытие от публикации до даты события, без дыр). Покупатель платит цену диапазона, покрывающего дату покупки; цена фиксируется при покупке (рост цены не влияет на купленные билеты). Промокод применяется к актуальной цене. Актуальная цена в Mini App + бот-анонсах | ✅ e2e `test_price_ranges_e2e.py` (диапазоны → покупка по цене → рост → промокод → free 409) + `TestPriceRanges` (services) + `TestPriceRangesAPI` (web) |

**Легенда:** ✅ реализовано · ⏸️ платформа не активна · 📌 запланировано · ⚠️ покрытие неполное

---

## Канонические сквозные сценарии (user journeys)

### 🧍 Пользователь — покупка и вход (F1→F2→F7)

```
Вход (TG Mini App / VK Mini App)
  → Найти мероприятие (лента / анонс)
  → Купить билет (платный/бесплатный)
  → Получить билет: код + QR в «Моих билетах», опционально ЛС
  → На входе предъявить код/QR организатору
  → (опц.) Отмена билета
```

**Ожидаемый результат:** билет виден в кабинете, код XXXX-XXXX предъявляется на входе.

### 🎪 Организатор — создание и продажа (F4→F5→F9→F7)

```
Вход (TG / VK, линковка F8 если кросс)
  → Подписка (F10: канал или пользователь)
  → Создать мероприятие (F4)
  → Подключить канал/VK-группу (F11/F12)
  → Опубликовать анонс (F5) + опц. продвижение (F6)
  → Создать промокод на скидку (F20: код, % или сумма, срок, лимит, вкл/выкл)
  → Покупатель вводит промокод при покупке → скидка в «Итого»
  → Задать цены по дате (F21: диапазоны с абсолютными датами, сплошное покрытие от публикации до даты)
  → Покупатель платит цену диапазона на дату покупки; цена фиксируется при покупке
  → Смотреть статистику (F9) + использованные промокоды
  → На входе: проверить код (F7) — ввести вручную или отсканировать QR (камера/фото, авто-check-in)
```

**Ожидаемый результат:** анонс в канале/группе, продажи в статистике, check-in по коду/QR.

### 🎛️ Супер-админ — управление платформой (F13→F14→F15→F16→F17)

```
Вход (TG)
  → Подписать канал (F13) + назначить админа (F14)
  → Глобальная статистика (F15)
  → Рассылка (F16) / Здоровье (F17)
```

**Ожидаемый результат:** каналы изолированы, админы назначены, мониторинг работает.

---

## Киллер-фича: кросс-платформенный цикл (F1 ↔ F7, обе стороны)

Одно мероприятие на всех площадках: код билета платформонезависим, check-in
по коду работает с любой площадки. Синхронизация счётчиков (sold/available)
мгновенная — общая БД.

### Направление A: купил в VK → проверил в TG (F1+VK → F7+TG)

```
VK-покупатель                     TG-организатор (канон)
    │                                   │
    │  F8 линковка TG↔VK ◄──────────────┘
    ▼                                   │
  F1 купить билет (VK Mini App)         │
    │                                   ▼
    ▼                                  F7 проверить код (TG)
  F2 билет: код + QR + ЛС VK            │
    └──────────────────────────────►  F9 статистика: продан = 1
```

**Реализовано:** `test_vk_e2e.test_full_vk_organizer_cycle` (купил в VK → проверил в TG).

### Направление B: купил в TG → проверил в VK (F1+TG → F7+VK) — симметрия

```
TG-покупатель (X-Skip-Auth)        VK-организатор (канон, линковка F8)
    │                                   │
  F1 купить билет (web, TG)             │
    │                                   ▼
    └──────────────────────────────►  F7 проверить код (VK Mini App)
```

**Реализовано:** `test_vk_e2e.test_tg_buy_vk_checkin_symmetry` (куплен в TG → validate/checkin в VK → 200).

### Синхронизация возврата: VK-возврат → TG-статистика (F3 + VK)

```
VK-покупатель                     TG-организатор (канон)
    │                                   │
  F1 купить билет (VK)                  │
    │                                   ▼
    │                                  F9 stats: sold=1, available-1
    │                                   │
  F3 возврат (VK, POST /tickets/{id}/cancel)
    │                                   ▼
    └──────────────────────────────►  F9 stats: sold=0, refunded=1, available вернулся
```

**Реализовано:** `test_vk_e2e.test_vk_buy_refund_sync_to_tg`.

---

## Как строить тесты и фичи на flows

### Правило
1. **Новая фича** должна вписываться в существующий flow (или создавать новый flow с обновлением матрицы).
2. **e2e-тест** = сквозной проход по flow (вход → шаги → ожидаемый результат).
3. Платформы тестируются симметрично: каждый flow для TG и VK, если платформа активна.

### Закрытые пробелы в e2e (2026-08-12, `feature/e2e-coverage-flows`)
- ✅ **F2** (Мои билеты + QR) — `tests/test_web_api.py::TestCabinetFlow` (реальный PNG)
- ✅ **F3** (Возврат билета) — `tests/test_web_api.py::TestCabinetFlow` (refund + место)
- ✅ **F7** (check-in) — TG-сим `tests/test_telegram_sim.py` (`/check`)
- ✅ **F5** (анонс) — TG-сим `tests/test_telegram_sim.py` (репост анонсов в канал)
- ✅ **F13-F17** (супер-админ) — `tests/test_super_admin_e2e.py` (web API, реальная БД)
- ✅ **F1 send-vk** — `tests/test_vk_e2e.py` (билет в ЛС VK от группы)
- ✅ **F1+F3 синхронизация VK→TG** — `tests/test_vk_e2e.py::test_vk_buy_refund_sync_to_tg` (покупка/возврат в VK, статистика в TG)
- ✅ **F1+F7 симметрия TG→VK** — `tests/test_vk_e2e.py::test_tg_buy_vk_checkin_symmetry` (куплен в TG → check-in в VK)
- ✅ **F18 пригласительные как ссылки** — `tests/test_organizer_e2e.py::test_invites_not_counted_in_sold` (не в sold, вычитаются из available, отмена возвращает места)

**Техдолг:** `/check` и `/repost_events` не зарегистрированы в режиме «только web»
(бот — только вход в кабинет). TG-sim вызывает хендлеры напрямую с реальным
Message; полный конвейер (фильтры/FSM) для этих команд не гоняется.

---

## Связанные документы
- `docs/vk-mini-app-plan.md` — план VK Mini App
- `docs/features-by-role.md` — матрица команд (TG-бот)
- `docs/testing-loop-research.md` — контур тестирования
- `docs/tasks.md` — учёт задач
