# Журнал ручного тестирования TicketBot

Формат записи:
- **Дата / время:** когда проводилось
- **Среда:** где (прод VPS / локально) и что запущено
- **Пункты плана:** какие разделы `docs/test-plan-manual.md` проверялись
- **Результат:** ✅ прошло / ❌ не прошло / ⚠️ замечание
- **Что не удалось протестировать:** и почему

Актуальный чек-лист — в `docs/test-plan-manual.md`. Здесь фиксируются результаты.

---

## 2026-08-05 — Сессия 1 (прод VPS)

**Среда:** прод VPS (`31.56.180.231`), контейнер `ticketbot-telegram`, супер-админ `541587295`, админ канала `6934798265`, канал `@testSellChann`.

**Автоматический регресс:** `pytest tests/` → **140 passed** ✅ (services, telegram, vk, max, web_api).

### Пункт 1 — Базовая подписка и ограничения

| Шаг | Что делали | Результат | Лог (event_type) |
|---|---|---|---|
| 1.1 | `/admin → 🟢 Подписать → @testSellChann 10 basic` | ✅ Активация BASIC прошла | `channel.created`, `subscription.activated` (tier basic, 10 дней, до 2026-08-15) |
| 1.2 | `/admin → 🟢 Подписать → @testSellChann 20 pro` (повторно на тот же канал) | ✅ Активация PRO прошла (апгрейд) | `subscription.activated` (tier pro, 20 дней, до 2026-08-25) |
| 1.3 | `/admin → 📋 Список каналов` | ✅ Канал виден, админ синхронизирован | `channel_admin.get_ids` (count 1), `channel_admins.synced` (added 6934798265) |
| доп. | `/admin → ℹ️ Инфо о канале` | ✅ Информация о канале доступна | `callback.received` (channel_info) |

**Итог пункта 1: ✅ пройдено.** Подписки активируются, канал создаётся, админ синхронизируется, список/инфо работают.

### ⚠️ Замечание
Для полного прохождения теста нужны **два разных канала**: один на **basic**, другой на **pro**. Сейчас оба тарифа активированы на один канал (`@testSellChann`, итоговый тариф — pro). Это не баг, но из-за этого нельзя проверить изоляцию тарифов и негативный тест «Basic не может платное» (пункт 3.3 плана).

### ❌ Что не удалось протестировать (пока)

| Пункт плана | Что не проверено | Причина |
|---|---|---|
| 1.2 (как отдельный канал) | Pro на **втором** канале | Только один канал использован |
| 2.1-2.3 | Создание бесплатного мероприятия на Basic | Нет канала с тарифом basic |
| 3.3 | Негативный тест «Basic не может платное» | Нет канала с тарифом basic |
| 4-6 | Покупка билетов, коды, вход | Не дошло |
| 7-8 | Super-admin функции, статистика | Не дошло |
